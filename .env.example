DATABASE_URL=postgresql://grocery_user:dev_password@localhost:5432/home_inventory
HOST=0.0.0.0
PORT=3000
RUST_LOG=info,home_inventory=debug
# Optional: set both to enable basic auth (leave unset for local dev)
# AUTH_USERNAME=admin
# AUTH_PASSWORD=changeme
